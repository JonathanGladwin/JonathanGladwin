rules: {
  'emueration-ruleset': {
    type: style,
      given: "$.info.title",
      then: {
        field: 'tags',
        function: enumeration,
        functionOptions: {
          values: ["title1","title2"]
        },
      },
    }
}